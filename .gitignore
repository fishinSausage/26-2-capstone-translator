# Only track CSV files in the repository
# Ignore everything else
*

# Allow directories so we can whitelist CSVs inside them
!*/

# Keep this .gitignore tracked
!.gitignore

# Allow CSV files at any depth
!*.csv
!**/*.csv

# End of .gitignore